# Documentation

All project documentation must be written in English.

Use the following structure:

- `dev/` for development, architecture, operations, and maintenance documents.
- `tasks/` for implementation tasks, investigations, and tracked technical work.

Do not place documentation or task documents in the repository root.
